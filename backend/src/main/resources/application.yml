spring:
  application:
    name: rag
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/venlab}
    username: ${POSTGRES_USER:raguser}
    password: ${POSTGRES_PASSWORD:ragpassword}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  ai:
    vectorstore:
      pgvector:
        table-name: vector_store
        dimensions: 768
        initialize-schema: true
    ollama:
      base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
      chat:
        options:
          model: ${CHAT_MODEL:llama3.2}
      embedding:
        options:
          model: ${EMBEDDING_MODEL:nomic-embed-text}
          num-ctx: 8192

app:
  available-models: ${AVAILABLE_MODELS:llama3.2,mistral,phi3}

server:
  port: ${PORT:8080}

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health
