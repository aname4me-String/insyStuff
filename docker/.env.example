# Database credentials
user=raguser
pass=ragpassword

# JWT / session signing secret
signin=change_me_to_a_long_random_secret

# Exposed ports (optional overrides)
APP_PORT=80
ADMINER_PORT=8081

# Ollama models (optional overrides)
OLLAMA_BASE_URL=http://ollama:11434
CHAT_MODEL=llama3.2
EMBEDDING_MODEL=nomic-embed-text
